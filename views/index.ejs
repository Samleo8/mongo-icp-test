<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<title>Cute and Cuddly Creatures</title>
	<link rel="stylesheet" href="index.css">
	<script src="index.lib.js" type="text/javascript"></script>
	<script src="index.js" type="text/javascript"></script>
</head>

<body class="text-center">
	<h1 class="title">
		Pandas or Penguins?
	</h1>
	<form action="/votes_form" method="POST">
		<input type="text" placeholder="Who art thou?" name="name">
		<input type="text" placeholder="A cuddly animal (eg. panda)?" name="animal">
		<div class="btn submit">Vote</div>
		<input type="submit" value="" class="noDisplay" />
	</form>

	<h2>Current Votes</h2>
	<div class="votes flex">
		<div class="vote-header noDisplay">
			<span>Name</span> |
			<span>Cuddly Animal</span>
		</div>
		<% if(votes.length<=0){ %>
			<div class="font-large italics">No votes yet!</div>
		<% } %>
		<% for(var i=0; i<votes.length; i++) {%>
		<div class="vote" id="voteID_<%= votes[i]._id %>">
			<span class="vote_name"><%= votes[i].name %></span> |
			<span class="vote_animal"><%= votes[i].animal %></span>
			<div class="pad-both"></div>
			<div class="btn-controls">
				<div class="btn change-name-btn disableSelect">Change Name</div>
				<div class="btn change-animal-btn disableSelect">Change Vote</div>
				<div class="btn delete-btn disableSelect">Delete</div>
			</div>
			<div class="italics font-small pad-top noDisplay">Note: The respective invasions result in the name and/or vote randomly being replaced with the invader.</div>
		</div>
		<% } %>
	</div>
</body>
</html>
