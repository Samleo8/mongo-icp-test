{
  "name": "mongo-test",
  "version": "1.0.0",
  "description": "Testing Express and MongoDB",
  "main": "server.js",
  "scripts": {
	"start": "export MONGO_PASSWORD=$(kubectl get secret --namespace default test-ibm-mongodb-dev -o jsonpath='{.data.password}' | base64 --decode; echo); nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Samuel Leong",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "ejs": "^2.6.1",
    "express": "^4.17.1",
    "mongodb": "^3.2.6"
  }
}
